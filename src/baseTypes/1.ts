// Перетворіть цей код на TypeScript, вказавши відповідні типи для всіх змінних.

// Variant 1:

// type Age = number;
// type Name = string;
// type Toggle = boolean;
// type Empty = null;
// type NotInitialize = any;
// type Callback = (arg: number) => number;

// let age: Age = 50;
// let name: Name = 'Max';
// let toggle: Toggle = true;
// let empty: Empty = null;
// let notInitialize: NotInitialize;
// let callback: Callback = (a) => { return 100 + a };

// Variant 2:

let age: number = 50;
let name: string = 'Max';
let toggle: boolean = true;
let empty: null = null;
let notInitialize: any; 
let callback: (arg: number) => number = (a) => { return 100 + a };


export {};
